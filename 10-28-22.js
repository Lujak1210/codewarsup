// Remove String Spaces

// Description:

// Simple, remove the spaces form the string, then return the resultant string.

function noSpace(x){return x.split(' ').join('')}
//function noSpace will take in a string(x) and return a splited and joined string, spaces removed.