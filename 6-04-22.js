// makeUpperCase

// Write a function that converts he input string to upper case.

// FIRST TRY!!!!!
function makeUpperCase(str) {
    return str.toUpperCase()
  }

// the Cowboy Way as an arrow function.


const makeUpperCase = str => str.toUpperCase();