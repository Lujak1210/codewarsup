// Bin to Decimal

// Complete the function which converts a binary number (given as a string) to a decimal number.

function binToDec(bin){
    return parseInt(bin,2);  //parseInt() braks down string in bin , and converts to the amount of decimals given, in this case 2 as it asks for a binary number.
}